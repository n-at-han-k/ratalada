# frozen_string_literal: true

# What the changed_file model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ChangedFile < ROM::Struct
  end
end
