# frozen_string_literal: true

# What the gitignore_template_info model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class GitignoreTemplateInfo < ROM::Struct
  end
end
