# frozen_string_literal: true

# What the label_template model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class LabelTemplate < ROM::Struct
  end
end
