# frozen_string_literal: true

# What the gpg_key model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class GPGKey < ROM::Struct
  end
end
