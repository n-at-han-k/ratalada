# frozen_string_literal: true

# What the issue_deadline model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class IssueDeadline < ROM::Struct
  end
end
