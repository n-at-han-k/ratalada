# frozen_string_literal: true

# What the files_response model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class FilesResponse < ROM::Struct
  end
end
