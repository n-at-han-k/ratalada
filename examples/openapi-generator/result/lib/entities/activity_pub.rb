# frozen_string_literal: true

# What the activity_pub model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ActivityPub < ROM::Struct
  end
end
