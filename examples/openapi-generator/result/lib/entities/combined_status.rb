# frozen_string_literal: true

# What the combined_status model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class CombinedStatus < ROM::Struct
  end
end
