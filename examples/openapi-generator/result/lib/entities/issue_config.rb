# frozen_string_literal: true

# What the issue_config model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class IssueConfig < ROM::Struct
  end
end
