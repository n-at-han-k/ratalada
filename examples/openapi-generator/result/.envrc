# A flake only sees tracked files, so a new one has to be at least intended.
# This file is copied into every generated project, and a fresh output
# directory is not a git repository at all -- hence the guard.
git rev-parse --git-dir >/dev/null 2>&1 && git add -N .

export BUNDLE_FORCE_RUBY_PLATFORM=true
use flake
