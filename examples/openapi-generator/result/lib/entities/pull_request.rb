# frozen_string_literal: true

# What the pull_request model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class PullRequest < ROM::Struct
  end
end
