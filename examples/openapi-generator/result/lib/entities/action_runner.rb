# frozen_string_literal: true

# What the action_runner model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ActionRunner < ROM::Struct
  end
end
