# frozen_string_literal: true

# What the node_info model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class NodeInfo < ROM::Struct
  end
end
