# frozen_string_literal: true

# What the user_heatmap_data model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class UserHeatmapData < ROM::Struct
  end
end
