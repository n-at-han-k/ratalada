# frozen_string_literal: true

# What the commit_status model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class CommitStatus < ROM::Struct
  end
end
