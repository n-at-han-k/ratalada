# frozen_string_literal: true

# What the public_key model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class PublicKey < ROM::Struct
  end
end
