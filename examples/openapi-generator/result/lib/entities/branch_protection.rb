# frozen_string_literal: true

# What the branch_protection model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class BranchProtection < ROM::Struct
  end
end
