# frozen_string_literal: true

# What the action_run model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ActionRun < ROM::Struct
  end
end
