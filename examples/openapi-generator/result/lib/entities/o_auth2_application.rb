# frozen_string_literal: true

# What the o_auth2_application model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class OAuth2Application < ROM::Struct
  end
end
