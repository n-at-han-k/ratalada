# frozen_string_literal: true

# What the register_runner_response model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class RegisterRunnerResponse < ROM::Struct
  end
end
