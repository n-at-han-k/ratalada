# frozen_string_literal: true

# What the action_task_response model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ActionTaskResponse < ROM::Struct
  end
end
