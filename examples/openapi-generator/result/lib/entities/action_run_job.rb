# frozen_string_literal: true

# What the action_run_job model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ActionRunJob < ROM::Struct
  end
end
