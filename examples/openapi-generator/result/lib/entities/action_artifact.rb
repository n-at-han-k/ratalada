# frozen_string_literal: true

# What the action_artifact model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ActionArtifact < ROM::Struct
  end
end
