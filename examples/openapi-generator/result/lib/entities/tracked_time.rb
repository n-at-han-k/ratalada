# frozen_string_literal: true

# What the tracked_time model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class TrackedTime < ROM::Struct
  end
end
