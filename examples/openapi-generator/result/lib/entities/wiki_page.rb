# frozen_string_literal: true

# What the wiki_page model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class WikiPage < ROM::Struct
  end
end
