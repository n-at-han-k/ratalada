# frozen_string_literal: true

# What the contents_response model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ContentsResponse < ROM::Struct
  end
end
