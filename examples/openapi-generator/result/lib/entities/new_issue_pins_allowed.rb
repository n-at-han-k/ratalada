# frozen_string_literal: true

# What the new_issue_pins_allowed model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class NewIssuePinsAllowed < ROM::Struct
  end
end
