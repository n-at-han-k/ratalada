# frozen_string_literal: true

# What the repo_collaborator_permission model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class RepoCollaboratorPermission < ROM::Struct
  end
end
