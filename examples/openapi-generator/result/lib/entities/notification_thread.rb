# frozen_string_literal: true

# What the notification_thread model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class NotificationThread < ROM::Struct
  end
end
