# frozen_string_literal: true

# What the file_delete_response model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class FileDeleteResponse < ROM::Struct
  end
end
