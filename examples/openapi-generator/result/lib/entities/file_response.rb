# frozen_string_literal: true

# What the file_response model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class FileResponse < ROM::Struct
  end
end
