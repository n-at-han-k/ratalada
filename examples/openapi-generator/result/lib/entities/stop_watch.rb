# frozen_string_literal: true

# What the stop_watch model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class StopWatch < ROM::Struct
  end
end
