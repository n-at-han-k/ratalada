# frozen_string_literal: true

# What the organization_permissions model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class OrganizationPermissions < ROM::Struct
  end
end
