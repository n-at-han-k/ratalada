# frozen_string_literal: true

# What the quota_group model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class QuotaGroup < ROM::Struct
  end
end
