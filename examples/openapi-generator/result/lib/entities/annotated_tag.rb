# frozen_string_literal: true

# What the annotated_tag model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class AnnotatedTag < ROM::Struct
  end
end
