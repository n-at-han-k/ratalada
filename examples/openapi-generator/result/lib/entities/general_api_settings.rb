# frozen_string_literal: true

# What the general_api_settings model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class GeneralAPISettings < ROM::Struct
  end
end
