# frozen_string_literal: true

# What the git_blob model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class GitBlob < ROM::Struct
  end
end
