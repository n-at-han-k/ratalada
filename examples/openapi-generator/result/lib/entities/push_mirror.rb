# frozen_string_literal: true

# What the push_mirror model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class PushMirror < ROM::Struct
  end
end
