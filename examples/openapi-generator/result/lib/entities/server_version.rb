# frozen_string_literal: true

# What the server_version model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class ServerVersion < ROM::Struct
  end
end
