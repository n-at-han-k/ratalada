# frozen_string_literal: true

# What the license_template_info model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class LicenseTemplateInfo < ROM::Struct
  end
end
