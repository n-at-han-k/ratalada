# frozen_string_literal: true

# What the watch_info model maps to. Add methods here: a struct is data, and these
# are the only place behaviour over that data belongs.
module Entities
  class WatchInfo < ROM::Struct
  end
end
