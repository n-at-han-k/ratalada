node_modules
.env

# Vite build output — one dir per environment, from config/vite.json.
/public/vite
/public/vite-dev
/public/vite-test

# vite_ruby's build cache.
/tmp

# The SQLite database the example creates on boot.
/config/database.sqlite*
